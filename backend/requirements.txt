# --- Core web app (everything runs with just these) ---
fastapi>=0.115
uvicorn[standard]>=0.30
sqlalchemy>=2.0.30
greenlet>=3.0          # required by SQLAlchemy async
pydantic>=2.7
pydantic-settings>=2.3
PyJWT>=2.8
email-validator>=2.1
bcrypt>=4.0
python-multipart>=0.0.9
aiosqlite>=0.20            # async SQLite driver (zero-config local default)
httpx>=0.27

# --- AI tutor: Claude and/or any OpenAI-compatible endpoint (Groq, OpenAI, Together...) ---
anthropic>=0.40
openai>=1.40

# --- Spaced repetition (FSRS-6, the 2026 state of the art) ---
fsrs>=4.0

# --- Content ingestion ---
beautifulsoup4>=4.12

# --- Postgres / pgvector (production mode via docker-compose) ---
asyncpg>=0.29
pgvector>=0.3

# --- Optional infra (gracefully skipped if absent/unset) ---
redis>=5.0
